{% extends "base.html.twig" %}
{% block content %}
    <div class="fh5co-page-title" style="background-image: url({{ asset('images/header.jpeg') }});">
        <div class="overlay"></div>
        <div class="container">
            <div class="row">
                <div class="col-md-12 animate-box">
                    <h1><span class="colored">Réinitialisation du mot de passe</span></h1>
                </div>
            </div>
        </div>
    </div>
    <div id="fh5co-testimoniall">
        <div class="container">
            <div class="row">
                <div class="col-md-6 col-md-offset-3">

                    {% for message in app.session.flashbag.get('info') %}
                        <p class="alert alert-danger">{{ message }}</p>
                    {% endfor %}
                    {{ form_start(form) }}
                    {{ form_errors(form) }}


                    <div class="form-group">
                        {{ form_label(form.password.first, "Veuillez saisir votre nouveau mot de passe") }}
                        {{ form_errors(form.password.first) }}
                        {{ form_widget(form.password.first, {'attr':{'class':'form-control'}}) }}
                    </div>
                    <div class="form-group">
                        {{ form_label(form.password.second, "Veuillez confirmer votre nouveau mot de passe") }}
                        {{ form_errors(form.password.second) }}
                        {{ form_widget(form.password.second, {'attr':{'class':'form-control'}}) }}
                    </div>
                    <div class="form-group">
                        <button type="submit" class="form-control">valider</button>
                    </div>
                    {{ form_end(form) }}
            </div>
        </div>
        </div>
    </div>
{% endblock %}
    {% block javascripts %}


    {% endblock %}